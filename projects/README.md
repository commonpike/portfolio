# Assets

Local asset root, used when `BASEDIR` in `../Portfolio.php` is empty. The real collection lives on the archive volume that `BASEDIR` points at.

Contents are not in git — only this README. See [../README.md](../README.md) for the folder and filename conventions.
